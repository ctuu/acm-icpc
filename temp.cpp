#include <iostream>
#include <algorithm>
#include <array>
#include <vector>
using namespace std;
struct Edge
{
    int fr, to, di;
    Edge(int fr, int to, int di): fr(fr), to(to), di(di) {}
};
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    return 0;
}