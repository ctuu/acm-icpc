#include <bits/stdc++.h>
// #include <cctype>
using namespace std;
int main()
{
    // ios_base::sync_with_stdio(0);
    // cin.tie(0);
    string text;
    array<int , 27> a;
    while (cin >> text)
    {
        a.fill(0);
        // for (auto i :text)
        //     ++a[toupper(i) - 'A'];
        for (auto i: a)
            cout << i << ' ';
    }
    
    return 0;
}
