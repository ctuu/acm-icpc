#include <iostream>
#include <algorithm>
#include <array>
#include <vector>
#define N 105
#define SHIFT 100000
#define S 1000
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    array<int, N> si, fi;
    si.fill(0);
    fi.fill(0);
    set<int> wei;
    for (int i = 0;i < n; ++i)
        cin >> si[i] >> fi[i];
    for (int i = 0; i < n; ++i)
    
    return 0;
}