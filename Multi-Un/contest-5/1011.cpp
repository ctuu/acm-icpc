#include <iostream>
#define N 100005
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    array<int, N> ar; 
    while (t--)
    {
        int n, k;
        cin >> n >> k;
        for (int i = 0; i < n; ++i)
            cin >> ar[i];
            
    return 0;
}