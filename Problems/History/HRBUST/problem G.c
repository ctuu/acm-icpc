#include <stdio.h>
int main(void)
{
    int t, a, b;
    int owi,xwi;
    char ch[3][3];
    char ne;
    scanf("%d", &t);
    while (t--)
    {
        owi = 0;
        xwi = 0;
        for (a = 0; a < 3; a++)
            for (b = 0; b < 3; b++)
                scanf("%s", &ch[a][b]);
        scanf("%s", &ne);

    }
}